Se utiliza para que git empiece a hacer el seguimiento del proyecto.
    git init 

Se utiliza para hacer seguimiento a un solo archivo.
    git add

Traslada el archivo/archivos que tengamos en el área de ensayo a el repositorio local, aquí ya se hace el respaldo del archivo. 
    git commit

    forma para realizar el commit:
        git commit -m "Descripción del commit."

Muestra el estado del directorio del trabajo, sus archivos y carpetas.
    git status -s


Agregar información de usuario.

    git config --global user.name "Nombre Usuario"

Agregar información de correo.
    git config --global user.email "correo@correo"

Ver el listado de todos los commits/copias que llevamos.

    git log --oneline

Regresar a un commit anterior.
    git reset --hard codigo_commit

Agregar todos los archivos, carpetas al seguimiento de git.
    git add .

Realizar un add y commit a la vez.

    git commit -am "descripcion"

Modificar alguna descripción de commit

    git commit --amend

Agregar nuestro repositorio que se encuentra en la nube:

    git remote add origin https://github.com/StevenNNZ/cursoGit.git

    (Esto depende netamente de la carpeta en la que estamos trabajando el repositorio de la nube, en este caso es cursoGit)

    

Comandos brindados por github para realizar la conexión al repositorio en la nube.
    git remote add origin https://github.com/StevenNNZ/cursoGit.git
    git branch -M main
    git push -u origin main

Traer los cambios nuevos que tenemos en la nube, al repositorio local.
    git pull

Agregar un tag:
    git tag 15-09-20v1 -m "version 1 del proyecto"

subir los tags a github:
    git push --tags

Los tags sirven para crear versiones de nuestro proyecto, así mismo en github permite descargar por versiones.